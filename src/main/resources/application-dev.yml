spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/shiro-jwt?characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: zhapeng
  redis:
    host: localhost
    port: 6379
    ## 连接超时时间（毫秒）
    timeout: 5000
    lettuce:
      pool:
        max-active: 8
        # 连接池阻塞等待时间（负值表示没有限制）
        max-wait: -1ms
        # 连接池中的最大空闲连接
        max-idle: 8
        # 连接池中最小空闲连接
        min-idle: 0

expoProtoco: http
expoIp: localhost
expoPort: 20090